package sample;

/** This method brings in the name, manufacturer and item type to display in the console. */
class Widget extends Product {
  Widget(String name, String manufacturer, ItemType type) {
    super(name, manufacturer, type);
  }
}
